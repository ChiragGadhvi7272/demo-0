import { ShipmentLinesInfo } from './shipment-lines-info.model';

describe('ShipmentLinesInfo', () => {
  it('should create an instance', () => {
    expect(new ShipmentLinesInfo()).toBeTruthy();
  });
});
