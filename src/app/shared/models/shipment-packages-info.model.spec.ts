import { ShipmentPackagesInfo } from './shipment-packages-info.model';

describe('ShipmentPackagesInfo', () => {
  it('should create an instance', () => {
    expect(new ShipmentPackagesInfo()).toBeTruthy();
  });
});
