import { ShipmentChargesInfo } from './shipment-charges-info.model';

describe('ShipmentChargesInfo', () => {
  it('should create an instance', () => {
    expect(new ShipmentChargesInfo()).toBeTruthy();
  });
});
