import { ReturnShipmentInfo } from './return-shipment-info.model';

describe('ReturnShipmentInfo', () => {
  it('should create an instance', () => {
    expect(new ReturnShipmentInfo()).toBeTruthy();
  });
});
