import { HazmatCommodityInfo } from './hazmat-commodity-info.model';

describe('HazmatCommodityInfo', () => {
  it('should create an instance', () => {
    expect(new HazmatCommodityInfo()).toBeTruthy();
  });
});
