import { ChildShipmentHeadersInfo } from './child-shipment-headers-info';

describe('ChildShipmentHeadersInfo', () => {
  it('should create an instance', () => {
    expect(new ChildShipmentHeadersInfo()).toBeTruthy();
  });
});
