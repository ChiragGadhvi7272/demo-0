import { AdditionalShipmentInfo } from './additional-shipment-info.model';

describe('AdditionalShipmentInfo', () => {
  it('should create an instance', () => {
    expect(new AdditionalShipmentInfo()).toBeTruthy();
  });
});
