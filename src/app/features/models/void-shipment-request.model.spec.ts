import { VoidShipmentRequest } from './void-shipment-request.model';

describe('VoidShipmentRequest', () => {
  it('should create an instance', () => {
    expect(new VoidShipmentRequest()).toBeTruthy();
  });
});
