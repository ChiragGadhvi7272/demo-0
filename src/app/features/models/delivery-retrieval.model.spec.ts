import { DeliveryRetrieval } from './delivery-retrieval.model';

describe('DeliveryRetrieval', () => {
  it('should create an instance', () => {
    expect(new DeliveryRetrieval()).toBeTruthy();
  });
});
