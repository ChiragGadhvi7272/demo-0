import { CarrierOrgAcctDetailsInfo } from './carrier-org-acct-details-info.model';

describe('CarrierOrgAcctDetailsInfo', () => {
  it('should create an instance', () => {
    expect(new CarrierOrgAcctDetailsInfo()).toBeTruthy();
  });
});
