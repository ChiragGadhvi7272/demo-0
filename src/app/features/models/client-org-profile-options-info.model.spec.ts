import { ClientOrgProfileOptionsInfo } from './client-org-profile-options-info.model';

describe('ClientOrgProfileOptionsInfo', () => {
  it('should create an instance', () => {
    expect(new ClientOrgProfileOptionsInfo()).toBeTruthy();
  });
});
