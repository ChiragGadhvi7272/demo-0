import { ShipMethodMappingInfo } from './ship-method-mapping-info.model';

describe('ShipMethodInfo', () => {
  it('should create an instance', () => {
    expect(new ShipMethodMappingInfo()).toBeTruthy();
  });
});
