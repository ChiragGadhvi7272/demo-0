import { PackingSlipRequest } from './packing-slip-request.model';

describe('PackingSlipRequest', () => {
  it('should create an instance', () => {
    expect(new PackingSlipRequest()).toBeTruthy();
  });
});
