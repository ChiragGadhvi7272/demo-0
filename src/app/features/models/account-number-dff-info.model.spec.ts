import { AccountNumberDffInfo } from './account-number-dff-info.model';

describe('AccountNumberDffInfo', () => {
  it('should create an instance', () => {
    expect(new AccountNumberDffInfo()).toBeTruthy();
  });
});
