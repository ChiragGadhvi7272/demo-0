import { AddressValidationRequest } from './address-validation-request.model';

describe('AddressValidationRequest', () => {
  it('should create an instance', () => {
    expect(new AddressValidationRequest()).toBeTruthy();
  });
});
