import { FreightCostTypesInfo } from './freight-cost-types-info.model';

describe('FreightCostTypesInfo', () => {
  it('should create an instance', () => {
    expect(new FreightCostTypesInfo()).toBeTruthy();
  });
});
