import { OrgDimensionsInfo } from './org-dimensions-info.model';

describe('OrgDimensions', () => {
  it('should create an instance', () => {
    expect(new OrgDimensionsInfo()).toBeTruthy();
  });
});
