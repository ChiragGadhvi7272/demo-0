import { FedExDirectDistInfo } from './fed-ex-direct-dist-info';

describe('FedExDirectDistInfo', () => {
  it('should create an instance', () => {
    expect(new FedExDirectDistInfo()).toBeTruthy();
  });
});
