import { ShipExecLabelInfo } from './ship-exec-label-info';

describe('ShipExecLabelInfo', () => {
  it('should create an instance', () => {
    expect(new ShipExecLabelInfo()).toBeTruthy();
  });
});
