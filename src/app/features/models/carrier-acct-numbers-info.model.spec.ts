import { CarrierAcctNumbersInfo } from './carrier-acct-numbers-info.model';

describe('CarrierAcctNumbersInfo', () => {
  it('should create an instance', () => {
    expect(new CarrierAcctNumbersInfo()).toBeTruthy();
  });
});
