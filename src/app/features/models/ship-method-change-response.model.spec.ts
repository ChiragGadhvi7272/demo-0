import { ShipMethodChangeResponse } from './ship-method-change-response.model';

describe('ShipMethodChangeResponse', () => {
  it('should create an instance', () => {
    expect(new ShipMethodChangeResponse()).toBeTruthy();
  });
});
