import { OrgProfileOptionsInfo } from './org-profile-options-info.model';

describe('OrgProfileOptionsInfo', () => {
  it('should create an instance', () => {
    expect(new OrgProfileOptionsInfo()).toBeTruthy();
  });
});
