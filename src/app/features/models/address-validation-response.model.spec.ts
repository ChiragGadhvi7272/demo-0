import { AddressValidationResponse } from './address-validation-response.model';

describe('AddressValidationResponse', () => {
  it('should create an instance', () => {
    expect(new AddressValidationResponse()).toBeTruthy();
  });
});
