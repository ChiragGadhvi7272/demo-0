import { ShipMethodChangeRequest } from './ship-method-change-request.model';

describe('ShipMethodChangeRequest', () => {
  it('should create an instance', () => {
    expect(new ShipMethodChangeRequest()).toBeTruthy();
  });
});
