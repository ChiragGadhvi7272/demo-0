import { ClientProfileSelectionsInfo } from './client-profile-selections-info.model';

describe('ClientProfileSelectionsInfo', () => {
  it('should create an instance', () => {
    expect(new ClientProfileSelectionsInfo()).toBeTruthy();
  });
});
