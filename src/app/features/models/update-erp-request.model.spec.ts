import { UpdateErpRequest } from './update-erp-request.model';

describe('UpdateErpRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateErpRequest()).toBeTruthy();
  });
});
