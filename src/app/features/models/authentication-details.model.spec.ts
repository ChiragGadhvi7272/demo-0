import { AuthenticationDetails } from './authentication-details.model';

describe('AuthenticationDetails', () => {
  it('should create an instance', () => {
    expect(new AuthenticationDetails()).toBeTruthy();
  });
});
