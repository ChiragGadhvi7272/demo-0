import { ApplicationSetupsInfo } from './application-setups-info.model';

describe('ApplicationSetupsInfo', () => {
  it('should create an instance', () => {
    expect(new ApplicationSetupsInfo()).toBeTruthy();
  });
});
