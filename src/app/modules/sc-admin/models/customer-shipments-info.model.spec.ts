import { CustomerShipmentsInfo } from './customer-shipments-info.model';

describe('CustomerShipmentsInfo', () => {
  it('should create an instance', () => {
    expect(new CustomerShipmentsInfo()).toBeTruthy();
  });
});
