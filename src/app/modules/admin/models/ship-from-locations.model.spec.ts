import { ShipFromLocations } from './ship-from-locations.model';

describe('ShipFromLocations', () => {
  it('should create an instance', () => {
    expect(new ShipFromLocations()).toBeTruthy();
  });
});
