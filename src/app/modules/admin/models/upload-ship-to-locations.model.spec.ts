import { UploadShipToLocations } from './upload-ship-to-locations.model';

describe('UploadShipToLocations', () => {
  it('should create an instance', () => {
    expect(new UploadShipToLocations()).toBeTruthy();
  });
});
